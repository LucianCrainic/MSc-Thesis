# Supervisor overview

Three-page discussion brief derived from the full critical review in
`analysis/analysis.tex`.

Build from this directory:

```bash
tectonic --keep-logs --keep-intermediates --reruns 1 overview.tex
```

The source uses the centralized bibliography at `thesis/references.bib`.
